---
name: Add async methods to WasmFunction interface
status: open
created: 2025-09-05T00:51:40Z
updated: 
github: https://github.com//issues/167
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Add async methods to WasmFunction interface

## Description
Add `callAsync()` method to the WasmFunction interface to enable asynchronous function invocation, aligning with existing WasiInstance.callAsync() for API consistency.

## Acceptance Criteria
- [ ] Add `CompletableFuture<WasmValue[]> callAsync(WasmValue... params)` to WasmFunction interface
- [ ] Add timeout variant `CompletableFuture<WasmValue[]> callAsync(Duration timeout, WasmValue... params)`
- [ ] Method signatures consistent with existing WasiInstance.callAsync() pattern
- [ ] Support for all WebAssembly value types in async context
- [ ] Proper error handling for async execution failures

## Technical Details
- **Implementation approach**: Add async methods alongside existing sync call() method
- **Key considerations**: 
  - Maintain thread safety across JNI/Panama implementations
  - Handle WebAssembly execution errors in CompletableFuture context
- **Code locations affected**: 
  - `wasmtime4j/src/main/java/ai/tegmentum/wasmtime4j/WasmFunction.java:22`

## Dependencies
- [ ] No external dependencies - can start immediately
- [ ] Reference existing WasiInstance.callAsync() implementation for consistency

## Effort Estimate
- Size: S
- Hours: 8-12 hours  
- Parallel: true (interface changes independent of other tasks)

## Definition of Done
- [ ] WasmFunction interface updated with async methods
- [ ] Support for timeout and cancellation
- [ ] Consistent error handling with CompletableFuture patterns
- [ ] Code compiles and maintains backward compatibility
- [ ] Code review completed
